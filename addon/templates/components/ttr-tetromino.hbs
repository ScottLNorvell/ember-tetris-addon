<rect
  x="0px"
  y="0px"
  width="{{box.w}}px"
  height="{{box.h}}px"
  style="fill:none;"
  />
{{#each positions as |pos|}}
  {{ttr-tetromino-square
      type=type
      pos=pos
      scale=scale
    }}
{{/each}}
